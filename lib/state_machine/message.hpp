#pragma once

namespace hyped::state_machine {

// State class containing all the states
enum class Message {
  // TODOLater Add the actual messages
  mForward,
  mFailure,
  mMitigated
};

}  // namespace hyped::state_machine