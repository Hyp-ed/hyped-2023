#pragma once

namespace hyped::state_machine {

// State class containing all the states
enum class State {
  // TODO Add the actual states
  kInitialState,
  state1,
  state2,
  state3,
  state4
};

}  // namespace hyped::state_machine